"""This is placeholder file in the event I ever decide to write tests. But I won't. So."""

from apps.football_pool import create_app

app = create_app()


def test_foo():
    assert "foo bar".title() == "Foo Bar"
